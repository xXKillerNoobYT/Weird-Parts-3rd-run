# Agent notes — Weird Parts / WiredPart

Flutter app: `app/`. Roadmap: `roadmap.md`. Setup: `docs/dev-setup.md`.

**Current focus:** foundation phases on the roadmap (post–Phase 1: search/filters/media next). Do not expand full WiredPart end-goal modules from `docs/end-goal/` unless the active phase requires it.

Hard constraints: local-first Drift/SQLite, PIN-gated catalog writes, soft-delete tombstones, no premature cloud/accounts/sync overbuild.
